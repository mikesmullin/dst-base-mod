package dst.types;

/**
 * The names of states are always references to indexes
 * so its important that you provide them consciously
 * as they are case-sensitive strings.
 */
abstract StateName(String){}
