package dst.types;

/**
 * The names of events are always references to indexes
 * so its important that you provide them consciously
 * as they are case-sensitive strings.
 */
typedef EventName = String;
